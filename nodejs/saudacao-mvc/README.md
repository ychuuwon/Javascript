# Saudação MVC com Express.js

Projeto exemplo utilizando `express-generator`, padrão MVC, EJS como view engine e formulário para saudação com faixa etária.

## Instalação

```bash
npm install
npm start
